<Window x:Class="VibeProxy.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VibeProxy.Windows"
        xmlns:viewModels="clr-namespace:VibeProxy.Windows.ViewModels"
        Title="VibeProxy"
        Width="540"
        Height="660"
        ResizeMode="CanMinimize"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#11131A" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#181B23" />
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#1E222C" />
        <SolidColorBrush x:Key="AccentBrush" Color="#7C5CFF" />
        <SolidColorBrush x:Key="AccentLightBrush" Color="#2D2750" />
        <SolidColorBrush x:Key="NeutralBorderBrush" Color="#2D303C" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="DangerChipBrush" Color="#41252A" />
        <SolidColorBrush x:Key="DangerChipBorderBrush" Color="#814651" />
        <SolidColorBrush x:Key="SuccessChipBrush" Color="#1F2E26" />
        <SolidColorBrush x:Key="SuccessChipBorderBrush" Color="#3B6B52" />
        <SolidColorBrush x:Key="InfoChipBrush" Color="#222F42" />
        <SolidColorBrush x:Key="InfoChipBorderBrush" Color="#325072" />
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}" >
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#252834" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="BorderBrush" Value="{DynamicResource NeutralBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1D1F29" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="MinWidth" Value="88" />
        </Style>
        <Style x:Key="DarkCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        </Style>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{StaticResource NeutralBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" Opacity="0.35" ShadowDepth="0" Color="#60000000" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ServerStatusChipStyle" TargetType="Border">
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Background" Value="{StaticResource SuccessChipBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SuccessChipBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsServerRunning}" Value="False">
                    <Setter Property="Background" Value="{StaticResource DangerChipBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource DangerChipBorderBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ServiceStatusChipStyle" TargetType="Border">
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Background" Value="{StaticResource InfoChipBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource InfoChipBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                    <Setter Property="Background" Value="{StaticResource SuccessChipBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource SuccessChipBorderBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{StaticResource WindowBackgroundBrush}">
        <StackPanel Margin="20" Orientation="Vertical" VerticalAlignment="Top" Background="{StaticResource WindowBackgroundBrush}" TextElement.Foreground="{StaticResource TextPrimaryBrush}">
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Server" FontWeight="Bold" FontSize="16" />
                            <TextBlock Text="{Binding ServerStatusText}" Margin="0,4,0,0" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <Border Style="{StaticResource ServerStatusChipStyle}" HorizontalAlignment="Right" Margin="0,0,0,8">
                                <TextBlock Text="{Binding ServerStateLabel}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                            </Border>
                            <Button Content="Toggle" Width="120" Style="{StaticResource PrimaryButtonStyle}" Click="OnToggleServer" />
                        </StackPanel>
                    </Grid>
                    <WrapPanel Margin="0,16,0,0" ItemHeight="32" ItemWidth="140" Orientation="Horizontal" MinHeight="32" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <CheckBox Content="Launch at login"
                                  IsChecked="{Binding LaunchAtLoginEnabled, Mode=OneWay}"
                                  Click="OnLaunchAtLoginClick"
                                  Style="{StaticResource DarkCheckBoxStyle}"
                                  Margin="0,0,10,10" />
                        <Button Content="Copy server URL" Click="OnCopyServerUrl" Margin="0,0,10,10" Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Content="Open auth folder" Click="OnOpenAuthFolder" Margin="0,0,10,10" Style="{StaticResource SecondaryButtonStyle}" />
                    </WrapPanel>
                </StackPanel>
            </Border>

            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Services" FontWeight="Bold" FontSize="15" />
                <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding ServiceCards}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="0,0,0,12" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type viewModels:ServiceCardViewModel}">
                            <Border Style="{StaticResource CardBorderStyle}" Padding="14">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Width="40" Height="40" Background="{StaticResource AccentLightBrush}" CornerRadius="8" VerticalAlignment="Center">
                                            <Image Source="{Binding IconPath}" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <StackPanel Margin="12,0,0,0">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Style="{StaticResource ServiceStatusChipStyle}" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding StatusChipText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" />
                                        </Border>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,8,0">
                                            <TextBlock Text="Authorizing" Margin="0,0,6,0" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                                            <ProgressBar Width="60" Height="6" IsIndeterminate="True" Foreground="{StaticResource AccentBrush}" Background="#262B38" />
                                        </StackPanel>
                                        <Button Content="Connect"
                                                Visibility="{Binding IsConnected, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                                Style="{StaticResource ActionButtonStyle}"
                                                Tag="{Binding Provider}"
                                                Click="OnConnectService"
                                                Margin="0,0,8,0" />
                                        <Button Content="Disconnect"
                                                Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                Style="{StaticResource ActionButtonStyle}"
                                                Tag="{Binding Provider}"
                                                Click="OnDisconnectService" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Logs" FontWeight="Bold" />
                <Border Margin="0,10,0,0" Padding="0" BorderThickness="1" BorderBrush="{StaticResource NeutralBorderBrush}" CornerRadius="8" Background="{StaticResource SurfaceBrush}">
                    <ListBox ItemsSource="{Binding LogLines}"
                             Height="180"
                             FontFamily="Consolas"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             Margin="4" />
                </Border>
            </StackPanel>

            <TextBlock Text="{Binding VersionText}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,20" />
        </StackPanel>
    </ScrollViewer>
</Window>
